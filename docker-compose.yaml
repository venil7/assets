services:
  darkruby-assets:
    # image: raspi4:6363/darkruby/assets:latest
    image: assets:latest
    environment:
      - ASSETS_DB=/data/assets.db
      - ASSETS_PORT=8080
      - ASSETS_JWT_SECRET=X7LGLA8D4frp
    volumes:
      # - ./data:/data
      - ./:/data
    ports:
      - 1990:8080
