services:
  darkruby-assets:
    image: ghcr.io/venil7/assets:${TAG}
    environment:
      - ASSETS_DB=/data/assets.db
      - ASSETS_JWT_SECRET=${ASSETS_JWT_SECRET}
    volumes:
      - ./:/data
    ports:
      - 8084:4020
