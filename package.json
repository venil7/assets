{
  "name": "@darkruby/assets",
  "private": true,
  "version": "1.0.3",
  "type": "module",
  "dependencies": {
    "@types/jsonwebtoken": "^9.0.9",
    "date-fns": "^4.1.0",
    "fp-ts": "^2.16.9",
    "io-ts-types": "^0.5.19",
    "jsonwebtoken": "^9.0.2",
    "recharts": "^2.15.2"
  },
  "devDependencies": {
    "bun-types": "^1.2.5",
    "sass-embedded": "^1.86.0",
    "typescript": "~5.8.2"
  },
  "scripts": {
    "web:dev": "cd packages/web && bun run dev",
    "web:check": "cd packages/web && bun run check",
    "web:build": "cd packages/web && bun run build",
    "backend:dev": "cd packages/backend && bun run dev",
    "backend:test": "cd packages/backend && bun test",
    "backend:check": "cd packages/backend && bun run check",
    "backend:build": "cd packages/backend && bun run build",
    "assets-core:check": "cd packages/assets-core && bun run check",
    "assets-core:test": "cd packages/assets-core && bun run test",
    "fp-express:check": "cd packages/fp-express && bun run check",
    "check": "bun run assets-core:check && bun run fp-express:check && bun run backend:check && bun run web:check",
    "build": "bun run web:build && bun run backend:build"
  },
  "workspaces": [
    "packages/*"
  ]
}
